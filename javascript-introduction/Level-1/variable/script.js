// let finalMoney;

// function calcDeposit(depositSize) {
//     if (depositSize < 50000) {
//         finalMoney = depositSize * 1.15 ** 2;
//     } else if (depositSize >= 50000) {
//         finalMoney = depositSize * 1.2 ** 2;
//     }
// }
// calcDeposit(10000);
// console.log(Math.round(finalMoney));

// let first;
// let total;

// function refill(first) {
//     total = first + first * 2 / 3 + first * 2 / 3 * 2 / 3;
//     console.log(Math.round(total));
// }
// refill(100);